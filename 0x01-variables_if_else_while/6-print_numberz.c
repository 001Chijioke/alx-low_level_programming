#include <stdio.h>
/**
 * main - prints the all single digits of base 10
 * Return: returns a value of zero
 */
int main(void)
{	
	int n;

	for (n = 0; n = 10; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
