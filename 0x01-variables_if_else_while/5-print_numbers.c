#include <stdio.h>
/**
 * main - prints all the single digit numbers of base 10
 * Return: returns zero
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
